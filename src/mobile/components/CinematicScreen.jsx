import ScreenScaffold from './ScreenScaffold';

/** @deprecated Use ScreenScaffold directly. Kept for backward compatibility. */
export default function CinematicScreen(props) {
  return <ScreenScaffold {...props} />;
}
